<script>

	// [here's a real-world example]
	// [make a note about what it's doing]

	// swap the password fields
	$('.login_pass-clear').show();
	$('.login_pass').hide();





	// [here's a real-world example]
	// [comment out unused-code]

	
	//  I don't think this is used anymore
	// but don't want to delete it yet

/*	function toggleResultHeight() {
  		var origHeight = $('.partial_result_hide').data('origHeight');

    	if (origHeight) {
    	    $('.partial_result_hide').removeData('origHeight');
    	    $('.partial_result_hide').animate({height: origHeight});
    	} else {
    	    origHeight = $('.partial_result_hide').height();
    	    $('.partial_result_hide').data('origHeight', origHeight);
    	    $('.partial_result_hide').animate({height: 300});
    	}
	};
*/


</script>